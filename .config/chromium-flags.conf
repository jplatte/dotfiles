# native wayland support
--enable-features=UseOzonePlatform --ozone-platform=wayland
# use the GPU more :)
--enable-gpu-rasterization --enable-zero-copy
# hardware video accelleration
--enable-features=VaapiVideoDecoder
